<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('../partials/head') %>
    <script src="js/hls-viewer.js" type="text/javascript"></script>
    <script src="https://cdn.jsdelivr.net/npm/hls.js@1"></script>
</head>

<body>
  <div id="credentials" style="display:none;" data="<%= credentials %>"></div>
  <div id="main" class="main-container">
    <div id="banner" class="banner">
      <span id="bannerText" class="text">Waiting for Broadcast to Begin</span>
    </div>
    <div id="videoContainer" class="video-container hidden">
      <video id="video" autoplay controls></video>
      <button type="button" class="btn btn-primary" data-target="#playVideoModal" data-toggle="modal">
        Switch to live mode
      </button>
    </div>
    <div id="play-video-container" class="hls-play-video hidden">
      <div class="button-container">
        <h3>The broadcast has started - Click to play the video</h3>
        <button id="play-video" class="btn-broadcast">Play Video</button>
      </div>

    </div>
  </div>
  <div id="playVideoModal" class="modal" tabindex="-1" role="dialog">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title">Switch to live mode</h5>
          <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
          </button>
        </div>
        <div class="modal-body">
          <p>Click to switch to live streaming mode</p>
        </div>
        <div class="modal-footer">
          <button id="go-live-btn" type="button" class="btn btn-primary">Go</button>
          <button id="close-btn" type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        </div>
      </div>
    </div>
  </div>
</body>

</html>